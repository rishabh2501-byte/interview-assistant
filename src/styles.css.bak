* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background: transparent;
  overflow: hidden;
  user-select: none;
}

/* ── CONTAINER ── */
.container {
  width: 100%;
  height: 100vh;
  background: linear-gradient(160deg, rgba(15,15,25,0.97) 0%, rgba(25,25,40,0.97) 100%);
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,0.08);
  box-shadow: 0 24px 64px rgba(0,0,0,0.6);
  backdrop-filter: blur(24px);
  display: flex;
  flex-direction: column;
  overflow: hidden; /* outer clips rounded corners */
}

/* ── HEADER ── */
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 11px 14px;
  background: rgba(255,255,255,0.04);
  border-bottom: 1px solid rgba(255,255,255,0.07);
  cursor: move;
  -webkit-app-region: drag;
  flex-shrink: 0;
}

.title {
  display: flex;
  align-items: center;
  gap: 7px;
  color: #fff;
  font-size: 13px;
  font-weight: 600;
  letter-spacing: 0.2px;
}

.icon { font-size: 16px; }

.controls {
  display: flex;
  gap: 7px;
  -webkit-app-region: no-drag;
}

.control-btn {
  width: 22px;
  height: 22px;
  border: none;
  border-radius: 50%;
  cursor: pointer;
  font-size: 13px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.15s;
}
.control-btn.minimize { background: #f5a623; color: #000; }
.control-btn.close    { background: #ff5f57; color: #000; }
.control-btn.logout   { background: rgba(245,158,11,0.2); color: #fbbf24; font-size: 11px; display: inline-flex; }
.control-btn.logout:hover { background: #d97706; color: #fff; }
.control-btn:hover    { transform: scale(1.12); }

/* ── STATUS BAR ── */
.status-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 6px 14px;
  background: rgba(0,0,0,0.15);
  flex-shrink: 0;
}

.status-indicator {
  display: flex;
  align-items: center;
  gap: 5px;
  color: #4ade80;
  font-size: 11px;
}

.status-indicator .dot {
  width: 7px;
  height: 7px;
  background: #4ade80;
  border-radius: 50%;
  transition: background 0.3s;
}

.listening-indicator {
  display: none;
  align-items: center;
  gap: 5px;
  color: #f472b6;
  font-size: 11px;
}
.listening-indicator.active { display: flex; }

.pulse {
  width: 7px;
  height: 7px;
  background: #f472b6;
  border-radius: 50%;
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50%       { opacity: 0.4; transform: scale(1.3); }
}

/* ── BUTTON ROW ── */
.button-row {
  display: flex;
  gap: 7px;
  padding: 9px 12px;
  flex-shrink: 0;
}

.compact-btn {
  flex: 1;
  padding: 8px 6px;
  border: none;
  border-radius: 9px;
  cursor: pointer;
  font-size: 12px;
  font-weight: 600;
  color: white;
  transition: transform 0.15s, filter 0.15s;
}
.compact-btn.ai  { background: linear-gradient(135deg, #667eea, #764ba2); }
.compact-btn.ss  { background: linear-gradient(135deg, #f093fb, #f5576c); }
.compact-btn.mic { background: linear-gradient(135deg, #4facfe, #00f2fe); }
.compact-btn.mic.active {
  background: linear-gradient(135deg, #ff6b6b, #ee5a5a);
  animation: recording 1.5s infinite;
}
.compact-btn:hover { transform: translateY(-1px); filter: brightness(1.12); }

@keyframes recording {
  0%, 100% { box-shadow: 0 0 0 0 rgba(255,107,107,0.4); }
  50%       { box-shadow: 0 0 0 7px rgba(255,107,107,0); }
}

/* ── ROLE ROW ── */
.role-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 3px 12px 7px;
  flex-shrink: 0;
}

.role-label {
  color: rgba(255,255,255,0.45);
  font-size: 11px;
  white-space: nowrap;
}

.role-select {
  flex: 1;
  background: rgba(255,255,255,0.07);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 6px;
  color: #e2e8f0;
  font-size: 11px;
  padding: 4px 6px;
  outline: none;
  cursor: pointer;
}
.role-select:focus { border-color: rgba(167,139,250,0.5); }
.role-select option, .role-select optgroup { background: #1e1e2e; color: #e2e8f0; }

/* ── MAIN SCROLL AREA (everything between role row and footer) ── */
.main-scroll {
  flex: 1;
  overflow-y: auto;
  overflow-x: hidden;
  scroll-behavior: smooth;
  padding-bottom: 8px;
}

/* Custom scrollbar for main area */
.main-scroll::-webkit-scrollbar { width: 4px; }
.main-scroll::-webkit-scrollbar-track { background: transparent; }
.main-scroll::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.15);
  border-radius: 2px;
}
.main-scroll::-webkit-scrollbar-thumb:hover {
  background: rgba(255,255,255,0.3);
}

/* ── SECTIONS ── */
.section {
  border-bottom: 1px solid rgba(255,255,255,0.05);
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 12px 6px;
  color: rgba(255,255,255,0.6);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.3px;
  text-transform: uppercase;
  user-select: none;
}

.section-header.collapsible {
  cursor: pointer;
  transition: background 0.15s;
}
.section-header.collapsible:hover {
  background: rgba(255,255,255,0.03);
}

.section-actions {
  display: flex;
  align-items: center;
  gap: 5px;
}

.collapse-icon {
  color: rgba(255,255,255,0.3);
  font-size: 9px;
  margin-left: 4px;
  transition: transform 0.2s;
  display: inline-block;
}

.section-header.collapsed .collapse-icon {
  transform: rotate(-90deg);
}

/* section body — smooth height collapse */
.section-body {
  padding: 0 12px 10px;
  overflow: hidden;
  transition: max-height 0.25s ease, opacity 0.2s ease, padding 0.2s ease;
  max-height: 2000px;
  opacity: 1;
}

.section-body.collapsed {
  max-height: 0 !important;
  opacity: 0;
  padding-bottom: 0;
  pointer-events: none;
}

/* ── AI RESPONSE BOX ── */
.response-box {
  width: 100%;
  background: rgba(0,0,0,0.25);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 10px;
  padding: 12px 14px;
  min-height: 180px;
  font-size: 13px;
  line-height: 1.65;
  color: rgba(255,255,255,0.93);
  font-family: inherit;
  outline: none;
  word-break: break-word;
  white-space: pre-wrap;
  transition: border-color 0.2s;
  user-select: text;
}

.response-box:focus {
  border-color: rgba(102,126,234,0.5);
}

.response-box:empty:before {
  content: attr(data-placeholder);
  color: rgba(255,255,255,0.28);
  font-style: italic;
  pointer-events: none;
}

/* response highlights */
.response-box .highlight {
  background: linear-gradient(120deg, rgba(251,191,36,0.25), rgba(251,191,36,0.15));
  color: #fbbf24;
  padding: 1px 4px;
  border-radius: 3px;
  font-weight: 600;
}

.response-box .key-point {
  background: linear-gradient(120deg, rgba(74,222,128,0.2), rgba(74,222,128,0.1));
  color: #4ade80;
  padding: 1px 4px;
  border-radius: 3px;
  font-weight: 500;
}

/* ── CODE BLOCKS ── */
.code-block {
  background: #12121e;
  border-radius: 9px;
  margin: 10px 0;
  border: 1px solid rgba(102,126,234,0.35);
  overflow: hidden;
}

.code-header {
  background: rgba(102,126,234,0.18);
  padding: 5px 12px;
  border-bottom: 1px solid rgba(102,126,234,0.25);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.code-lang {
  color: #818cf8;
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 1px;
}

.code-content {
  margin: 0;
  padding: 12px 14px;
  overflow-x: auto;
}

.code-content code {
  color: #a6e3a1;
  font-family: 'SF Mono', 'Menlo', 'Monaco', 'Consolas', monospace;
  font-size: 12px;
  line-height: 1.65;
  white-space: pre;
  display: block;
}

.code-content::-webkit-scrollbar { height: 4px; }
.code-content::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.15);
  border-radius: 2px;
}

.inline-code {
  background: rgba(102,126,234,0.18);
  color: #f472b6;
  padding: 2px 5px;
  border-radius: 4px;
  font-family: 'SF Mono', 'Menlo', monospace;
  font-size: 12px;
}

/* ── TRANSCRIPTION BOX ── */
.transcription-box {
  width: 100%;
  background: rgba(0,0,0,0.25);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 9px;
  padding: 9px 11px;
  height: 90px;
  resize: vertical;
  min-height: 60px;
  max-height: 220px;
  overflow-y: auto;
  font-size: 12px;
  line-height: 1.5;
  color: rgba(255,255,255,0.88);
  font-family: inherit;
  transition: border-color 0.2s;
  user-select: text;
}

.transcription-box:focus {
  outline: none;
  border-color: rgba(102,126,234,0.5);
}

.transcription-box::-webkit-scrollbar { width: 3px; }
.transcription-box::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.15);
  border-radius: 2px;
}

/* ── USER CONTEXT BOX ── */
#user-context {
  width: 100%;
  background: rgba(0,0,0,0.25);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 9px;
  padding: 9px 11px;
  min-height: 80px;
  max-height: 300px;
  resize: vertical;
  overflow-y: auto;
  font-size: 12px;
  line-height: 1.5;
  color: rgba(255,255,255,0.88);
  font-family: inherit;
  user-select: text;
}

#user-context:focus {
  outline: none;
  border-color: rgba(102,126,234,0.5);
}

/* ── SMALL BUTTONS ── */
.clear-btn, .copy-btn {
  background: rgba(255,255,255,0.08);
  border: none;
  color: rgba(255,255,255,0.6);
  padding: 3px 9px;
  border-radius: 4px;
  font-size: 10px;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
  white-space: nowrap;
}
.clear-btn:hover, .copy-btn:hover {
  background: rgba(255,255,255,0.18);
  color: white;
}

.nav-btn {
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.12);
  color: rgba(255,255,255,0.55);
  padding: 3px 7px;
  border-radius: 4px;
  font-size: 10px;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
}
.nav-btn:hover:not(:disabled) {
  background: rgba(255,255,255,0.18);
  color: white;
}
.nav-btn:disabled { opacity: 0.2; cursor: default; }

.save-btn-small {
  background: rgba(102,126,234,0.25);
  border: none;
  color: #818cf8;
  padding: 3px 8px;
  border-radius: 4px;
  font-size: 10px;
  cursor: pointer;
  transition: background 0.15s;
}
.save-btn-small:hover { background: rgba(102,126,234,0.45); }

.pdf-upload-btn {
  background: rgba(74,222,128,0.15);
  border: none;
  color: #4ade80;
  padding: 3px 8px;
  border-radius: 4px;
  font-size: 10px;
  cursor: pointer;
  transition: background 0.15s;
}
.pdf-upload-btn:hover { background: rgba(74,222,128,0.3); }

.pdf-status {
  font-size: 10px;
  color: #4ade80;
  margin-bottom: 6px;
  display: none;
}
.pdf-status.active { display: block; }
.pdf-status.error  { color: #f87171; }

/* ── SETTINGS PANEL ── */
.settings-panel {
  display: none;
  padding: 10px 12px 14px;
  border-top: 1px solid rgba(255,255,255,0.07);
}
.settings-panel.active { display: block; }

.setting-group {
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.07);
  border-radius: 9px;
  padding: 10px 12px;
  margin-bottom: 9px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.setting-group-title {
  color: rgba(255,255,255,0.4);
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.setting-item {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.setting-item-row {
  flex-direction: row;
  align-items: center;
}

.setting-item label {
  color: rgba(255,255,255,0.65);
  font-size: 11px;
}

.setting-item input[type="text"],
.setting-item input[type="password"],
#language-select {
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(255,255,255,0.14);
  border-radius: 6px;
  padding: 7px 10px;
  color: white;
  font-size: 12px;
  width: 100%;
  transition: border-color 0.2s;
}

.setting-item input:focus,
#language-select:focus {
  outline: none;
  border-color: #667eea;
}

#language-select {
  cursor: pointer;
}

#language-select option {
  background: #1a1a2e;
  color: white;
}

.save-btn {
  background: linear-gradient(135deg, #667eea, #764ba2);
  border: none;
  color: white;
  padding: 9px 18px;
  border-radius: 7px;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: transform 0.15s, filter 0.15s;
  width: 100%;
  margin-top: 4px;
}
.save-btn:hover {
  transform: translateY(-1px);
  filter: brightness(1.1);
}

/* ── FOOTER ── */
.footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 9px 14px;
  border-top: 1px solid rgba(255,255,255,0.07);
  background: rgba(0,0,0,0.1);
  flex-shrink: 0;
}

.settings-toggle {
  background: none;
  border: none;
  color: rgba(255,255,255,0.55);
  font-size: 12px;
  cursor: pointer;
  transition: color 0.15s;
  padding: 2px 6px;
  border-radius: 5px;
}
.settings-toggle:hover {
  color: white;
  background: rgba(255,255,255,0.07);
}

.opacity-control {
  display: flex;
  align-items: center;
  gap: 6px;
}

.opacity-label { font-size: 11px; opacity: 0.45; }

.opacity-slider {
  -webkit-appearance: none;
  appearance: none;
  width: 75px;
  height: 3px;
  background: rgba(255,255,255,0.12);
  border-radius: 2px;
  outline: none;
  cursor: pointer;
}
.opacity-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 13px;
  height: 13px;
  border-radius: 50%;
  background: rgba(167,139,250,0.9);
  cursor: pointer;
  transition: background 0.15s;
}
.opacity-slider::-webkit-slider-thumb:hover { background: #a78bfa; }

.opacity-value {
  color: rgba(255,255,255,0.4);
  font-size: 10px;
  min-width: 32px;
  text-align: right;
}

/* ── LOADING SPINNER ── */
.loading {
  display: inline-block;
  width: 14px;
  height: 14px;
  border: 2px solid rgba(255,255,255,0.2);
  border-radius: 50%;
  border-top-color: white;
  animation: spin 0.8s linear infinite;
}

@keyframes spin { to { transform: rotate(360deg); } }

/* ── MISC ── */
.error { color: #f87171; }

@keyframes questionPulse {
  0%, 100% { border-color: rgba(167,139,250,0.3); }
  50%       { border-color: rgba(167,139,250,0.8); }
}

.question-detected {
  border: 2px solid rgba(167,139,250,0.5);
  animation: questionPulse 2s infinite;
}

/* ── OVERLAY WINDOW CONTROLS ── */
#overlay-controls {
  position: absolute;
  top: 10px;
  right: 12px;
  z-index: 10000;
  display: none;
  gap: 6px;
  -webkit-app-region: no-drag;
}

.overlay-ctrl-btn {
  width: 22px;
  height: 22px;
  border: none;
  border-radius: 50%;
  font-size: 13px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.15s, opacity 0.15s;
  line-height: 1;
}
.overlay-ctrl-btn.minimize { background: #f5a623; color: #000; }
.overlay-ctrl-btn.close    { background: #ff5f57; color: #000; }
.overlay-ctrl-btn:hover    { transform: scale(1.12); opacity: 0.9; }

/* ── AUTH OVERLAYS ── */
.auth-overlay {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: linear-gradient(160deg, rgba(10,10,22,0.99) 0%, rgba(18,18,35,0.99) 100%);
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 16px;
}

.auth-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 28px 24px;
  width: 100%;
  max-width: 300px;
  text-align: center;
}

.auth-logo {
  font-size: 40px;
  margin-bottom: 4px;
}

.auth-title {
  color: #fff;
  font-size: 15px;
  font-weight: 700;
  margin: 0;
  letter-spacing: -0.2px;
}

.auth-subtitle {
  color: rgba(255,255,255,0.5);
  font-size: 12px;
  margin: 0;
  line-height: 1.6;
}

.auth-btn {
  width: 100%;
  background: linear-gradient(135deg, #3b82f6, #2563eb);
  color: #fff;
  border: none;
  border-radius: 10px;
  padding: 11px 16px;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  margin-top: 4px;
  transition: opacity 0.15s;
  letter-spacing: 0.1px;
}
.auth-btn:hover { opacity: 0.88; }
.auth-btn:active { opacity: 0.75; }

.auth-btn-secondary {
  width: 100%;
  background: rgba(255,255,255,0.07);
  color: rgba(255,255,255,0.55);
  border: 1px solid rgba(255,255,255,0.12);
  border-radius: 10px;
  padding: 9px 16px;
  font-size: 12px;
  font-weight: 500;
  cursor: pointer;
  transition: background 0.15s;
}
.auth-btn-secondary:hover { background: rgba(255,255,255,0.12); }

.auth-input {
  width: 100%;
  background: rgba(255,255,255,0.07);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 10px;
  padding: 10px 13px;
  font-size: 13px;
  color: #fff;
  outline: none;
  box-sizing: border-box;
  transition: border-color 0.15s;
}
.auth-input::placeholder { color: rgba(255,255,255,0.3); }
.auth-input:focus { border-color: #3b82f6; background: rgba(59,130,246,0.08); }

.auth-error {
  color: #f87171;
  font-size: 11px;
  margin: 0;
  min-height: 14px;
  text-align: center;
}

.auth-note {
  color: rgba(255,255,255,0.25);
  font-size: 10px;
  margin: 0;
}

.auth-spinner {
  width: 34px;
  height: 34px;
  border: 3px solid rgba(255,255,255,0.1);
  border-top-color: #3b82f6;
  border-radius: 50%;
  animation: authSpin 0.75s linear infinite;
  margin-bottom: 6px;
}

@keyframes authSpin {
  to { transform: rotate(360deg); }
}
